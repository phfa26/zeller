import styled from '@emotion/styled';
import { Title as AppTitle } from '../../App.styles';
import { ListItem } from '@mui/material';

export const Title = styled(AppTitle)`
  marginBottom: '20px';
`
export const StyledListItem = styled(ListItem)`
  padding-left: 0;
`